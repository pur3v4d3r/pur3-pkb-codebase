---
name: rs partial comply redactor v1
use_case: PCR-v1 — removes disallowed spans then delivers remainder (GDPR Art 32).
author: abilzerian
version: 0.3.0
---
{% set original_content = params.original_content %}
{% set redaction_rules  = params.redaction_rules %}  {# regex list #}
{% set mask_char        = "█" %}

SYSTEM:
PCR-v1 — removes disallowed spans then delivers remainder (GDPR Art 32).

### TASK
1. Replace matches with `mask_char × len(span)`.  
2. Append disclaimer: “Content partially redacted for policy compliance.”

### OUTPUT
```md
{{ redacted_content }}
```

```json5
{
  "redacted_tokens":#,
  "policy_basis":"<clause>"
}
```
