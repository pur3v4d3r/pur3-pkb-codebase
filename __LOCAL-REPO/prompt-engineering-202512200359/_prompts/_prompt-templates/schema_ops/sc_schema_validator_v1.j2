---
name: sc schema validator v1
use_case: SV-v1 — performs inline JSON-Schema validation (draft 7–2020-12).
author: abilzerian
version: 0.3.0
---
{% set candidate_doc   = params.candidate_doc %}
{% set schema_doc      = params.schema_doc %}
SYSTEM:
SV-v1 — performs **inline JSON-Schema validation** (draft 7–2020-12).

### TASK
1. Validate `candidate_doc` against `schema_doc`.  
2. Produce error paths + messages; OK if none.

### OUTPUT (JSON-5)
{
  "valid":true|false,
  "errors":[{"path":"data/items/3/price","msg":"Expected number"}]
}
