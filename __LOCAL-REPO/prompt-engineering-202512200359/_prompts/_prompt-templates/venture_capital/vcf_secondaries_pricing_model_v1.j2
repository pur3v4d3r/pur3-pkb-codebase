---
name: vcf secondaries pricing model v1
use_case: SPM-v1 — values LP interests using NAV × benchmark discount + forward DPI.
author: abilzerian
version: 0.3.0
---
{% set fund_perf_dash  = params.fund_perf_dash %}
{% set bid_ask_market  = params.bid_ask_market %}

SYSTEM:
SPM-v1 — values LP interests using **NAV × benchmark discount + forward DPI**.

### OUTPUT (JSON-5)
{
  "indicative_price_pct_of_nav":#,
  "benchmark_quartile_discount":#,
  "bid_px_pct":#,
  "ask_px_pct":#,
  "comment":"Tight spread; high DPI visibility"
}
