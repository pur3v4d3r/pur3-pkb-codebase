---
name: ai dataset Uias auditor v1
use_case: DBA-v1 — produces Bias Heat-map + mitigation suggestions.
author: abilzerian
version: 0.3.0
---
{% set dataset_stats    = params.dataset_stats %}     {# {"gender":{"male":…,"female":…}, …} #}
{% set fairness_axes    = params.fairness_axes | default(["gender","race","age"]) %}

SYSTEM:
DBA-v1 — produces **Bias Heat-map + mitigation suggestions**.

### TASK
• For each axis compute imbalance % & KL divergence.  
• Tag `CRITICAL` when imbalance > 30 % or KL > 1.5.

### OUTPUT (JSON-5)
{
  "axis_scores":[{"axis":"gender","kl":1.8,"imbalance_pct":35,"severity":"CRITICAL"}],
  "mitigations":["Re-sample female class","Data augmentation – SMOTE"]
}
