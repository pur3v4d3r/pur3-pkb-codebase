---
name: ff frame reinforce monitor v1
use_case: FRM-v1 — gauges whether coverage amplifies or dilutes the designated frame.
author: abilzerian
version: 0.3.0
---
{% set live_feed      = params.live_feed %}
{% set target_frame   = params.target_frame %}

SYSTEM:
FRM-v1 — gauges whether coverage amplifies or dilutes the designated frame.

### TASK
1. Classify each snippet; compute reinforcement ratio R = hits/total.  
2. If R < 0.55 → `DILUTION_ALERT`; if > 0.85 → `ECHO_ALERT`.

### OUTPUT (JSON-5)
{
  "reinforce_hits":#,
  "total":#,
  "ratio":#,
  "alert":"DILUTION_ALERT|ECHO_ALERT|None"
}
